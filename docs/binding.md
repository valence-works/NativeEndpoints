# Binding

> **Draft.** This page is a stub. The library is pre-release and its documentation is being written
> alongside the implementation. See the [README](../README.md) for the current, accurate picture.

Planned contents: binding precedence, the supported type set, body modes, registering a value
binder, and why the binder is deliberately narrower than `RequestDelegateFactory`'s.
